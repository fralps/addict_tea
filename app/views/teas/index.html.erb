<%= render 'teas/dashboard' %>
<div id="content">
	<!-- Begin Page Content -->
	<div class="container-fluid">

		<!-- Page Heading -->
		<div class="d-sm-flex align-items-center justify-content-between mb-4">
			<h2 class="h3 mb-0 text-gray-800">Dashboard</h2>
		</div>
		<p class="mt-3">
			<% if current_user.teas.count == nil %>
			<h4><i class="fas fa-box-open mr-1"></i>Vous n'avez pas encore de thés dans votre boîte à thés 🌿</h4><%= link_to(new_tea_path, class: "btn btn-success btn-icon-split") do %>
			<span class="icon text-white-40">
				<i class="fas fa-plus-square"></i>
			</span>
			<span class="text">Ajouter un thé</span>
			<% end %>
			<% elsif current_user.teas.count == 1 %>
			<h4><i class="fas fa-box-open mr-1"></i>Ma boîte à thés : <%= current_user.teas.count %> thé 🌿</h4>
			<% else %>
			<h4><i class="fas fa-box-open mr-1"></i>Ma boîte à thés : <%= current_user.teas.count %> thés 🌿</h4>
			<% end %>
		</p>
		<div class="row">
			<% current_user.teas.each do |t| %>
			<div class="col col-md-4 mb-4">
				<div class="card card-me h-100 py-2 shadow">
					<%= link_to(tea_path(t.id)) do %>
					<div class="card-body">
						<div class="row no-gutters align-items-center">
							<div class="col mr-2">
								<div class="font-weight-bold text-success text-uppercase mb-1"><%= t.name %></div>
								<div class="h5 mb-0 text-xs text-gray-800"><%= t.fermentation %></div>
								<div class="h5 mb-0 mt-2 text-xs text-gray-800"><i class="fas fa-clock mr-1"></i><%= t.infusion_time %> minutes d'infusion</div>
							</div>
							<div class="col-auto">
								<i class="fas fa-leaf fa-2x text-success"></i>
							</div>
						</div>
					</div>
					<% end %>
				</div>
			</div>
			<% end %>
		</div>
	</div>
	<!-- End of Main Content -->
</div>
